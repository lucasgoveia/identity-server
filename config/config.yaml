server:
  port: 1323
  host: "localhost"

database:
  provider: "postgres"

hashing:
  algorithm: "argon2"

postgres:
    url: "postgres://postgres:password@localhost:5432/identity-server"

mailer:
  provider: 'smtp'

cache:
  provider: "redis"

redis:
  url: "localhost:6379"
  username: ""
  password: ""

smtp:
  host: "localhost"
  port: 25
  username: "guest"
  password: "guest"
  from: "no-reply@localhost"
  from_name: "Identity Server"
  tls: false
  default_credentials: false